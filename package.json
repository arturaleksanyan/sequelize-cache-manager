{
  "name": "sequelize-cache-manager",
  "version": "0.4.0",
  "description": "Lightweight cache manager for Sequelize models with TTL, stale-while-revalidate, and auto-refresh",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --runInBand",
    "lint": "eslint . --ext .ts",
    "prepare": "npm run build"
  },
  "files": ["dist"],
  "keywords": ["sequelize", "cache", "ttl", "stale-while-revalidate"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "sequelize": "^6.0.0"
  }
}

